from setuptools import setup, find_packages

setup(
    name="bigcode_eval",  # Replace with your own package name
    description="be2a44c",
    version="1.0",  # Replace with your package version
    packages=find_packages(),
)
